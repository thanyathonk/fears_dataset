# Core dependencies (pinned for systems with GCC < 9.3, e.g. CentOS 7)
# On CentOS 7 / GCC < 9.3: pip install numpy --only-binary :all: && pip install -r requirements.txt --only-binary :all:
numpy>=1.26.0,<2.0
pandas>=2.1.0,<3.0
matplotlib>=3.7.0
aiohttp>=3.9.0
datasets>=2.15.0
duckdb>=0.9.2
huggingface_hub>=0.22.0
httpx[http2]>=0.26.0
joblib>=1.3.0
loguru>=0.7.0
pandera>=0.18.0
polars>=0.20.0
pyarrow>=14.0.0
pydantic>=2.3.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
rich>=13.4.0
tenacity>=8.2.0
tqdm>=4.65.0
typer[all]>=0.9.0

# Chemistry (RDKit)
rdkit-pypi>=2022.9.5
